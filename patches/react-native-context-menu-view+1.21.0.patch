diff --git a/node_modules/react-native-context-menu-view/index.d.ts b/node_modules/react-native-context-menu-view/index.d.ts
index 25c92fd..0269e1f 100644
--- a/node_modules/react-native-context-menu-view/index.d.ts
+++ b/node_modules/react-native-context-menu-view/index.d.ts
@@ -80,6 +80,13 @@ export interface ContextMenuProps extends ViewProps {
    * Handle when the menu is cancelled and closed
    */
   onCancel?: () => void;
+  /**
+   * Handle when the menu's dismissal animation has fully completed (iOS only).
+   * Fires after both action selection and cancellation. This is the safe
+   * point to mutate the wrapped view hierarchy — doing so earlier (e.g. in
+   * onPress) can strand the iOS preview platter mid-animation.
+   */
+  onDismiss?: () => void;
   /**
    * The background color of the preview. This is displayed underneath your view. Set this to transparent (or another color) if the default causes issues.
    */
diff --git a/node_modules/react-native-context-menu-view/ios/ContextMenu.m b/node_modules/react-native-context-menu-view/ios/ContextMenu.m
index f1b3603..4f15cb7 100644
--- a/node_modules/react-native-context-menu-view/ios/ContextMenu.m
+++ b/node_modules/react-native-context-menu-view/ios/ContextMenu.m
@@ -14,6 +14,7 @@ - (UIView *) view {
 RCT_EXPORT_VIEW_PROPERTY(onPress, RCTBubblingEventBlock)
 RCT_EXPORT_VIEW_PROPERTY(onPreviewPress, RCTBubblingEventBlock)
 RCT_EXPORT_VIEW_PROPERTY(onCancel, RCTBubblingEventBlock)
+RCT_EXPORT_VIEW_PROPERTY(onDismiss, RCTDirectEventBlock)
 RCT_EXPORT_VIEW_PROPERTY(actions, NSArray<ContextMenuAction>)
 RCT_EXPORT_VIEW_PROPERTY(disabled, BOOL)
 RCT_CUSTOM_VIEW_PROPERTY(previewBackgroundColor, UIColor, ContextMenuView) {
diff --git a/node_modules/react-native-context-menu-view/ios/ContextMenuView.h b/node_modules/react-native-context-menu-view/ios/ContextMenuView.h
index 63964b2..e055438 100644
--- a/node_modules/react-native-context-menu-view/ios/ContextMenuView.h
+++ b/node_modules/react-native-context-menu-view/ios/ContextMenuView.h
@@ -16,6 +16,7 @@
 @property (nullable, nonatomic, copy) RCTBubblingEventBlock onPress;
 @property (nullable, nonatomic, copy) RCTBubblingEventBlock onPreviewPress;
 @property (nullable, nonatomic, copy) RCTBubblingEventBlock onCancel;
+@property (nullable, nonatomic, copy) RCTDirectEventBlock onDismiss;
 @property (nullable, nonatomic, copy) NSArray<ContextMenuAction*>* actions;
 @property (nullable, nonatomic, copy) UIColor* previewBackgroundColor;
 @property (nonatomic, assign) BOOL disabled;
diff --git a/node_modules/react-native-context-menu-view/ios/ContextMenuView.m b/node_modules/react-native-context-menu-view/ios/ContextMenuView.m
index e10a830..ab8a06b 100644
--- a/node_modules/react-native-context-menu-view/ios/ContextMenuView.m
+++ b/node_modules/react-native-context-menu-view/ios/ContextMenuView.m
@@ -103,6 +103,22 @@ - (void)contextMenuInteraction:(UIContextMenuInteraction *)interaction willEndFo
   if (_cancelled && self.onCancel) {
     self.onCancel(@{});
   }
+
+  // Fire onDismiss once the dismissal animation has fully completed. This is
+  // the deterministic point at which callers may safely mutate the wrapped
+  // view hierarchy without stranding the preview platter mid-animation.
+  __weak ContextMenuView *weakSelf = self;
+  void (^notifyDismiss)(void) = ^{
+    ContextMenuView *strongSelf = weakSelf;
+    if (strongSelf != nil && strongSelf.onDismiss) {
+      strongSelf.onDismiss(@{});
+    }
+  };
+  if (animator != nil) {
+    [animator addCompletion:notifyDismiss];
+  } else {
+    notifyDismiss();
+  }
 }
 
 - (UIPreviewParameters *)getPreviewParams {
