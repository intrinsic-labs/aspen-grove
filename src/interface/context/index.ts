/**
 * Context Module
 *
 * Exports all context providers and hooks for the interface layer.
 */

export { AppContextProvider, useAppContext, type AppContextValue, type AppContextProviderProps } from './app-context';
